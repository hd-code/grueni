{
  "name": "grueni-app",
  "version": "0.0.1",
  "private": true,
  "main": "index.js",
  "scripts": {
    "make:ssl": "openssl req -nodes -new -x509 -days 1024 -keyout ssl/server.key -out ssl/server.cert",
    "start": "ts-node ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/node": "^12.12.47",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  },
  "dependencies": {
    "express": "^4.17.1",
    "qrcode-terminal": "^0.12.0"
  }
}
